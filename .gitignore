# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
# Intermediate SSR bundle for the prerenderer. Normally removed at the end of
# the build; this keeps it out of git if a build fails partway through.
.ssr-dist
*.local

# Holds VITE_SITE_URL. See .env.example.
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
